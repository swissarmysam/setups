{
  "name": "setups",
  "version": "0.0.1",
  "description": "Get inspired by some amazing setups!",
  "main": "index.js",
  "scripts": {
    "start": "parcel *.html",
    "build:parcel": "parcel build *.html --public-url ./",
    "build": "npm run clean && npm run build:parcel",
    "clean": "rm -rf dist/*"
  },
  "author": "Sam Rampling",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.32.7"
  },
  "dependencies": {
    "airtable": "^0.10.1",
    "bulma": "^0.9.2"
  }
}
